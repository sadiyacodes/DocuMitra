# embed.py Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `backend/ingestion/embed.py` — encode new `Chunk` objects into 384-dim vectors using `bge-small-en-v1.5` and upsert them to a Supabase pgvector table, skipping already-stored chunks.

**Architecture:** Single public function `embed_chunks(chunks, client, model=None) -> int`. Internally: query pgvector for existing chunk_ids, batch-encode only new chunks via `SentenceTransformer.encode()`, upsert rows with `client.table("chunks").upsert(rows).execute()`. Model is a lazy singleton injected as an optional parameter for testability.

**Tech Stack:** Python 3.11+, `sentence-transformers`, `supabase` Python client, `numpy`, pytest.

---

## File Structure

| File | Action | Purpose |
|------|--------|---------|
| `backend/ingestion/embed.py` | Modify (was stub) | Full implementation, built up across tasks |
| `tests/ingestion/test_embed.py` | Create | TDD tests, built up across tasks |

---

## Task 1: Module skeleton + _get_model

**Files:**
- Modify: `backend/ingestion/embed.py`
- Create: `tests/ingestion/test_embed.py`

- [ ] **Step 1: Write failing tests**

Create `tests/ingestion/test_embed.py`:

```python
from __future__ import annotations

from unittest.mock import MagicMock, patch

from backend.ingestion.embed import EMBEDDING_MODEL, TABLE_NAME, _get_model


def test_constants_defined():
    assert EMBEDDING_MODEL == "BAAI/bge-small-en-v1.5"
    assert TABLE_NAME == "chunks"


def test_get_model_returns_sentence_transformer():
    with patch("backend.ingestion.embed.SentenceTransformer") as mock_st:
        mock_st.return_value = MagicMock()
        # Reset the singleton so it reloads
        import backend.ingestion.embed as embed_mod
        embed_mod._model = None
        result = _get_model()
    mock_st.assert_called_once_with(EMBEDDING_MODEL)
    assert result is mock_st.return_value


def test_get_model_singleton_cached():
    with patch("backend.ingestion.embed.SentenceTransformer") as mock_st:
        mock_st.return_value = MagicMock()
        import backend.ingestion.embed as embed_mod
        embed_mod._model = None
        r1 = _get_model()
        r2 = _get_model()
    assert r1 is r2
    mock_st.assert_called_once()
```

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -v
```
Expected: `ImportError: cannot import name 'EMBEDDING_MODEL'`

- [ ] **Step 3: Replace backend/ingestion/embed.py with module skeleton**

```python
"""Encodes text chunks into vectors using BAAI/bge-small-en-v1.5."""
from __future__ import annotations

import logging

import numpy as np
from sentence_transformers import SentenceTransformer
from supabase import Client

from backend.ingestion.chunker import Chunk

EMBEDDING_MODEL = "BAAI/bge-small-en-v1.5"
EMBEDDING_DIM = 384
TABLE_NAME = "chunks"
BATCH_SIZE = 64

log = logging.getLogger(__name__)

_model: SentenceTransformer | None = None


def _get_model() -> SentenceTransformer:
    """Lazy singleton: load bge-small-en-v1.5 once and keep it warm."""
    global _model
    if _model is None:
        _model = SentenceTransformer(EMBEDDING_MODEL)
    return _model
```

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -v
```
Expected: 3 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add backend/ingestion/embed.py tests/ingestion/test_embed.py
git commit -m "feat: add embed.py skeleton with _get_model singleton"
```

---

## Task 2: _fetch_existing_ids

**Files:**
- Modify: `backend/ingestion/embed.py`
- Modify: `tests/ingestion/test_embed.py`

- [ ] **Step 1: Write failing tests**

Append to `tests/ingestion/test_embed.py`:

```python
from backend.ingestion.embed import TABLE_NAME, _fetch_existing_ids


def _make_client(existing: list[str]) -> MagicMock:
    """Mock Supabase client returning given chunk_ids from SELECT."""
    mock = MagicMock()
    mock.table.return_value.select.return_value.in_.return_value.execute.return_value.data = [
        {"chunk_id": cid} for cid in existing
    ]
    return mock


def test_fetch_existing_ids_returns_matching_set():
    client = _make_client(["abc", "def"])
    result = _fetch_existing_ids({"abc", "def", "ghi"}, client)
    assert result == {"abc", "def"}


def test_fetch_existing_ids_empty_table_returns_empty_set():
    client = _make_client([])
    result = _fetch_existing_ids({"abc", "def"}, client)
    assert result == set()


def test_fetch_existing_ids_empty_input_returns_empty_set():
    client = _make_client([])
    result = _fetch_existing_ids(set(), client)
    assert result == set()


def test_fetch_existing_ids_queries_correct_table():
    client = _make_client([])
    _fetch_existing_ids({"abc"}, client)
    client.table.assert_called_once_with(TABLE_NAME)


def test_fetch_existing_ids_selects_chunk_id_column():
    client = _make_client([])
    _fetch_existing_ids({"abc"}, client)
    client.table.return_value.select.assert_called_once_with("chunk_id")
```

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -k "fetch_existing" -v
```
Expected: `ImportError: cannot import name '_fetch_existing_ids'`

- [ ] **Step 3: Implement _fetch_existing_ids in backend/ingestion/embed.py**

Add after `_get_model`:

```python
def _fetch_existing_ids(ids: set[str], client: Client) -> set[str]:
    """Return the subset of ids already present in pgvector."""
    if not ids:
        return set()
    resp = client.table(TABLE_NAME).select("chunk_id").in_("chunk_id", list(ids)).execute()
    return {row["chunk_id"] for row in resp.data}
```

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -k "fetch_existing" -v
```
Expected: 5 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add backend/ingestion/embed.py tests/ingestion/test_embed.py
git commit -m "feat: implement _fetch_existing_ids"
```

---

## Task 3: _upsert_rows

**Files:**
- Modify: `backend/ingestion/embed.py`
- Modify: `tests/ingestion/test_embed.py`

- [ ] **Step 1: Write failing tests**

Append to `tests/ingestion/test_embed.py`:

```python
import numpy as np

from backend.ingestion.chunker import Chunk
from backend.ingestion.embed import TABLE_NAME, _upsert_rows


def _make_chunk(
    chunk_id: str = "testchunk00000001",
    bbox: tuple | None = (0.0, 0.0, 595.0, 842.0),
) -> Chunk:
    return Chunk(
        chunk_id=chunk_id,
        pdf_id="testpdf123456789",
        filename="doc.pdf",
        page_number=1,
        text="This is some chunk text.",
        token_count=5,
        language="en",
        bbox=bbox,
    )


def _make_upsert_client() -> MagicMock:
    mock = MagicMock()
    mock.table.return_value.upsert.return_value.execute.return_value = MagicMock()
    return mock


def test_upsert_rows_calls_table_upsert_execute():
    chunk = _make_chunk()
    vectors = np.zeros((1, 384))
    client = _make_upsert_client()
    _upsert_rows([chunk], vectors, client)
    client.table.assert_called_once_with(TABLE_NAME)
    client.table.return_value.upsert.assert_called_once()
    client.table.return_value.upsert.return_value.execute.assert_called_once()


def test_upsert_rows_row_contains_all_chunk_fields():
    chunk = _make_chunk()
    vectors = np.ones((1, 384))
    client = _make_upsert_client()
    _upsert_rows([chunk], vectors, client)
    rows = client.table.return_value.upsert.call_args[0][0]
    assert len(rows) == 1
    row = rows[0]
    assert row["chunk_id"] == chunk.chunk_id
    assert row["pdf_id"] == chunk.pdf_id
    assert row["filename"] == chunk.filename
    assert row["page_number"] == chunk.page_number
    assert row["text"] == chunk.text
    assert row["token_count"] == chunk.token_count
    assert row["language"] == chunk.language


def test_upsert_rows_embedding_is_list_of_floats():
    chunk = _make_chunk()
    vectors = np.ones((1, 384)) * 0.5
    client = _make_upsert_client()
    _upsert_rows([chunk], vectors, client)
    rows = client.table.return_value.upsert.call_args[0][0]
    emb = rows[0]["embedding"]
    assert isinstance(emb, list)
    assert len(emb) == 384
    assert all(isinstance(v, float) for v in emb)


def test_upsert_rows_bbox_tuple_stored_as_list():
    chunk = _make_chunk(bbox=(0.0, 0.0, 595.0, 842.0))
    vectors = np.zeros((1, 384))
    client = _make_upsert_client()
    _upsert_rows([chunk], vectors, client)
    rows = client.table.return_value.upsert.call_args[0][0]
    assert rows[0]["bbox"] == [0.0, 0.0, 595.0, 842.0]


def test_upsert_rows_none_bbox_stored_as_none():
    chunk = _make_chunk(bbox=None)
    vectors = np.zeros((1, 384))
    client = _make_upsert_client()
    _upsert_rows([chunk], vectors, client)
    rows = client.table.return_value.upsert.call_args[0][0]
    assert rows[0]["bbox"] is None


def test_upsert_rows_multiple_chunks():
    chunks = [_make_chunk(f"chunk{i:016d}") for i in range(3)]
    vectors = np.zeros((3, 384))
    client = _make_upsert_client()
    _upsert_rows(chunks, vectors, client)
    rows = client.table.return_value.upsert.call_args[0][0]
    assert len(rows) == 3
    assert rows[0]["chunk_id"] == "chunk0000000000000"
    assert rows[2]["chunk_id"] == "chunk0000000000002"
```

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -k "upsert" -v
```
Expected: `ImportError: cannot import name '_upsert_rows'`

- [ ] **Step 3: Implement _upsert_rows in backend/ingestion/embed.py**

Add after `_fetch_existing_ids`:

```python
def _upsert_rows(
    chunks: list[Chunk],
    vectors: np.ndarray,
    client: Client,
) -> None:
    """Build row dicts and upsert to Supabase."""
    rows = [
        {
            "chunk_id":    c.chunk_id,
            "pdf_id":      c.pdf_id,
            "filename":    c.filename,
            "page_number": c.page_number,
            "text":        c.text,
            "token_count": c.token_count,
            "language":    c.language,
            "bbox":        list(c.bbox) if c.bbox is not None else None,
            "embedding":   vectors[i].tolist(),
        }
        for i, c in enumerate(chunks)
    ]
    client.table(TABLE_NAME).upsert(rows).execute()
```

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -k "upsert" -v
```
Expected: 6 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add backend/ingestion/embed.py tests/ingestion/test_embed.py
git commit -m "feat: implement _upsert_rows"
```

---

## Task 4: embed_chunks integration

**Files:**
- Modify: `backend/ingestion/embed.py`
- Modify: `tests/ingestion/test_embed.py`

- [ ] **Step 1: Write failing tests**

Append to `tests/ingestion/test_embed.py`:

```python
from backend.ingestion.embed import embed_chunks


def _make_full_client(existing_ids: list[str] | None = None) -> MagicMock:
    """Mock Supabase client for embed_chunks: handles both SELECT and UPSERT."""
    existing_ids = existing_ids or []
    mock = MagicMock()
    # SELECT path (_fetch_existing_ids)
    mock.table.return_value.select.return_value.in_.return_value.execute.return_value.data = [
        {"chunk_id": cid} for cid in existing_ids
    ]
    # UPSERT path (_upsert_rows)
    mock.table.return_value.upsert.return_value.execute.return_value = MagicMock()
    return mock


def _make_model(n_chunks: int = 1) -> MagicMock:
    """Mock SentenceTransformer that returns a zero numpy array."""
    mock = MagicMock()
    mock.encode.return_value = np.zeros((n_chunks, 384))
    return mock


def test_embed_chunks_empty_list_returns_zero():
    client = _make_full_client()
    result = embed_chunks([], client, model=_make_model(0))
    assert result == 0


def test_embed_chunks_empty_list_does_not_call_model():
    mock_model = _make_model(0)
    embed_chunks([], _make_full_client(), model=mock_model)
    mock_model.encode.assert_not_called()


def test_embed_chunks_all_existing_returns_zero():
    chunk = _make_chunk()
    client = _make_full_client(existing_ids=[chunk.chunk_id])
    result = embed_chunks([chunk], client, model=_make_model(0))
    assert result == 0


def test_embed_chunks_all_existing_does_not_call_model():
    chunk = _make_chunk()
    mock_model = _make_model(0)
    client = _make_full_client(existing_ids=[chunk.chunk_id])
    embed_chunks([chunk], client, model=mock_model)
    mock_model.encode.assert_not_called()


def test_embed_chunks_new_chunk_encodes_and_upserts():
    chunk = _make_chunk()
    client = _make_full_client(existing_ids=[])
    mock_model = _make_model(1)
    result = embed_chunks([chunk], client, model=mock_model)
    assert result == 1
    mock_model.encode.assert_called_once()


def test_embed_chunks_encode_called_with_texts():
    chunk = _make_chunk()
    client = _make_full_client()
    mock_model = _make_model(1)
    embed_chunks([chunk], client, model=mock_model)
    call_kwargs = mock_model.encode.call_args
    assert chunk.text in call_kwargs[0][0]


def test_embed_chunks_encode_called_with_normalize_embeddings():
    chunk = _make_chunk()
    client = _make_full_client()
    mock_model = _make_model(1)
    embed_chunks([chunk], client, model=mock_model)
    _, kwargs = mock_model.encode.call_args
    assert kwargs.get("normalize_embeddings") is True


def test_embed_chunks_encode_called_with_batch_size():
    from backend.ingestion.embed import BATCH_SIZE
    chunk = _make_chunk()
    client = _make_full_client()
    mock_model = _make_model(1)
    embed_chunks([chunk], client, model=mock_model)
    _, kwargs = mock_model.encode.call_args
    assert kwargs.get("batch_size") == BATCH_SIZE


def test_embed_chunks_partial_existing_only_encodes_new():
    old_chunk = _make_chunk(chunk_id="oldchunk0000000001")
    new_chunk = _make_chunk(chunk_id="newchunk0000000002")
    client = _make_full_client(existing_ids=[old_chunk.chunk_id])
    mock_model = _make_model(1)
    result = embed_chunks([old_chunk, new_chunk], client, model=mock_model)
    assert result == 1
    texts_encoded = mock_model.encode.call_args[0][0]
    assert new_chunk.text in texts_encoded
    assert len(texts_encoded) == 1


def test_embed_chunks_uses_lazy_singleton_when_model_not_provided():
    chunk = _make_chunk()
    client = _make_full_client()
    mock_model = _make_model(1)
    with patch("backend.ingestion.embed._get_model", return_value=mock_model):
        embed_chunks([chunk], client)
    mock_model.encode.assert_called_once()
```

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -k "embed_chunks" -v
```
Expected: `ImportError: cannot import name 'embed_chunks'`

- [ ] **Step 3: Implement embed_chunks in backend/ingestion/embed.py**

Add after `_upsert_rows`:

```python
def embed_chunks(
    chunks: list[Chunk],
    client: Client,
    model: SentenceTransformer | None = None,
) -> int:
    """Encode new chunks and upsert to Supabase. Returns count of newly stored chunks."""
    if not chunks:
        return 0

    if model is None:
        model = _get_model()

    existing_ids = _fetch_existing_ids({c.chunk_id for c in chunks}, client)
    new_chunks = [c for c in chunks if c.chunk_id not in existing_ids]

    if not new_chunks:
        return 0

    vectors = model.encode(
        [c.text for c in new_chunks],
        batch_size=BATCH_SIZE,
        normalize_embeddings=True,
    )
    _upsert_rows(new_chunks, vectors, client)
    return len(new_chunks)
```

- [ ] **Step 4: Run full test suite**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ingestion/test_embed.py -v
```
Expected: All tests PASSED (≥ 24 tests).

- [ ] **Step 5: Run combined suite to verify no regressions**

Run:
```bash
cd /Users/sadiya/projects/DocuMitra && pytest tests/ -q --tb=short 2>&1 | tail -5
```
Expected: all tests pass (74 extract+chunker + embed tests).

- [ ] **Step 6: Commit**

```bash
git add backend/ingestion/embed.py tests/ingestion/test_embed.py
git commit -m "feat: implement embed_chunks — encode and upsert to pgvector"
```

---

## Self-Review

**Spec coverage:**
- ✅ `EMBEDDING_MODEL="BAAI/bge-small-en-v1.5"`, `EMBEDDING_DIM=384`, `TABLE_NAME="chunks"`, `BATCH_SIZE=64` — Task 1
- ✅ `_get_model` lazy singleton — Task 1
- ✅ `_fetch_existing_ids` queries by chunk_id set — Task 2
- ✅ `_upsert_rows` builds row dicts, bbox as list or None, embedding as list — Task 3
- ✅ `embed_chunks` early-exits on empty input, filters existing, batch-encodes, upserts — Task 4
- ✅ `normalize_embeddings=True` — Task 4
- ✅ `model` injected as optional param (None → uses singleton) — Task 4
- ✅ Returns count of newly stored chunks — Task 4

**Type consistency:**
- `_fetch_existing_ids(ids: set[str], client: Client) -> set[str]` — used in `embed_chunks` with `{c.chunk_id for c in chunks}` ✅
- `_upsert_rows(chunks: list[Chunk], vectors: np.ndarray, client: Client) -> None` — called in `embed_chunks` with `new_chunks` and `vectors` from `model.encode(...)` ✅
- `embed_chunks(...) -> int` — returns `len(new_chunks)` ✅

**No placeholders found.**
